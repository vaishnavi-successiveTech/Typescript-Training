const Dashboard = () => {
  return (
    <div>
      This is our Dashboard
      <ul>
        <li>
          Extend the application to include nested routes for a more complex
          user interface.
        </li>
        <li>
          Create a new section of your application (e.g., a Dashboard) with
          multiple sub-pages (e.g., Dashboard, Profile, Settings).
        </li>
        <li>
          Implement nested routing within this section using nested routes.
        </li>
        <li>
          Add navigation links to switch between the sub-pages within the
          Dashboard section.
        </li>
        <li>Customize the content displayed on each sub-page.</li>
      </ul>
    </div>
  );
};
export default Dashboard;
