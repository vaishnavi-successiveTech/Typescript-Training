import React from 'react'

const HomePage = () => {
  return (
    <div>
        <p> .Create a Modal component that displays a dialog with content. Users should be able to open and close the modal.
             Write tests using React Testing Library to verify that the modal appears and disappears when triggered..</p>
      
    </div>
  )
}

export default HomePage
