import React from 'react'

const HomePage = () => {
  return (
    <div>
        <p> You are given a LoginForm component with username and password fields and a submit button. 
            Write test cases using React Testing Library to verify that the input fields accept user input and the form submission works as expected.</p>
      
    </div>
  )
}

export default HomePage
