import React from 'react'

const HomePage = () => {
  return (
    <div>
        <p> Create a Counter component that shows a number with increment and decrement buttons. 
            Write tests using React Testing Library to verify that the counter updates correctly on button clicks.</p>
      
    </div>
  )
}

export default HomePage
